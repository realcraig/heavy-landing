# Heavy Landing

Cinematic Starship launch and Super Heavy tower-catch simulation.

Play: https://realcraig.github.io/heavy-landing/

This repository contains only the finished static export in `site/` and its GitHub Pages publishing workflow. The source workspace is maintained separately.

Publishing a new export to `main` deploys the production site. No build runs on GitHub; the checked local export is uploaded directly.
